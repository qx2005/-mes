# BSQ 工业互联网平台 — 项目说明文档

> 文档基于 `bsq_usr` 运行包目录结构与配置文件整理，适用于快速了解平台组成、启动方式与业务模块。

---

## 1. 项目概述

`bsq_usr` 是一个 **Windows 下一键部署的工业互联网平台运行包**，不是典型的源码工程，而是将 MES、IoT、数据可视化及全部中间件 **打包在一起的可运行环境**。

平台定位：**制造执行（MES）+ 物联网（IoT）+ 数据分析（BI）+ 标识解析代理** 的一体化演示/教学/实训环境。

| 子系统 | 产品 | 版本 | 作用 |
|--------|------|------|------|
| MES 主平台 | BSQ-MES (`bsq-admin.jar`) | 3.8.2 | 生产、质量、仓储、设备等制造业务 |
| IoT 控制台 | ThingsBoard (`thingsboard.jar`) | 3.3.4 | 设备接入、规则链、边缘计算 |
| 数据可视化 | DataEase (`CoreApplication.jar`) | 2.10.5 | 大屏、报表、仪表盘 |
| Web 入口 | Nginx + `dist_mes` | 1.22.1 | MES 前端静态资源与 API 反向代理 |

**默认访问地址：** `http://127.0.0.1:82`  
**默认账号：** `user01` / `123456`

**平台内跳转：**

- 登录 MES 后，右上角 **「数据可视化控制台」** → DataEase
- 右上角 **「IOT 控制台」** → ThingsBoard（也可作为边缘计算平台使用）

---

## 2. 系统架构

```
用户浏览器
  MES :82  |  ThingsBoard :8090  |  DataEase :8081
       |
       v
  Nginx :82  (dist_mes, /prod-api -> :8080)
       |
       +-- bsq-admin :8080 (MES)
       +-- thingsboard :8090 (IoT)
       +-- CoreApplication :8081 (DataEase)
       |
  ActiveMQ(MQTT:1893)  MinIO(:9000)  Redis(:6379)
       |
  MySQL :3306/bsq-mes  |  PostgreSQL :5432/thingsboard  |  MySQL8 :8306/dataeasev2
```

**请求路径说明：**

1. 浏览器访问 `:82` → Nginx 返回 Vue 前端（`dist_mes`）
2. 前端 API 走 `/prod-api/` → 反代到 `127.0.0.1:8080`（`bsq-admin`）
3. MES 右上角可跳转 DataEase 与 ThingsBoard

---

## 3. 目录结构

```
bsq_usr/
├── 一键启动工业互联网平台服务.bat   # 主启动脚本
├── bsq-admin.jar                    # MES 后端
├── thingsboard.jar                  # IoT 平台
├── CoreApplication.jar              # DataEase 后端
├── dist_mes/                        # MES 前端
├── opt/dataease2.0/                 # DataEase 数据与配置
├── data/                            # ThingsBoard SQL 与 demo 数据
├── nginx-1.22.1/                    # Web 服务器
├── jdk/                             # Java 8
├── jdk-21/                          # Java 21
├── activemq/                        # 消息队列（含 MQTT）
├── minio/                           # 对象存储
├── redis/                           # 缓存
├── mysql/                           # MySQL 5.x（MES）
├── mysql-8.0.30/                    # MySQL 8（DataEase）
├── pgsql/                           # PostgreSQL（ThingsBoard）
├── tools/dbeaver/                   # 数据库工具（可选）
├── Readme.txt                       # 简要部署说明
└── 项目说明文档.md                  # 本文档
```

**重要说明：**

- 本目录 **不包含 Java/Vue 源码**，只有编译后的 JAR 和静态前端
- 二次开发需单独获取 BSQ-MES 源码工程
- 部署前需将 `opt` 文件夹复制到项目根目录

---

## 4. 部署与启动

### 4.1 环境要求

- 操作系统：Windows
- 解压路径 **不能含中文或特殊字符**
- 建议内存：8GB 及以上

### 4.2 首次部署步骤

参考 `Readme.txt`：

1. 将 `bsq_usr` 解压到纯英文路径
2. 将 `opt` 文件夹复制至项目根目录
3. 双击 `一键启动工业互联网平台服务.bat`，等待数分钟
4. 浏览器访问 `http://127.0.0.1:82`，使用 `user01` / `123456` 登录

### 4.3 一键启动顺序

| 顺序 | 脚本/命令 | 服务 |
|------|-----------|------|
| 1 | `01MQTTRun.bat` | ActiveMQ（MQTT 1893） |
| 2 | `02minIORun.bat` | MinIO |
| 3 | `10RedisRun.bat` | Redis |
| 4 | `20MySQLRun.bat` | MySQL 5.x |
| 5 | `20MySQL8Run.bat` | MySQL 8.0 |
| 6 | `21pgsqlRun.bat` | PostgreSQL |
| 7 | `java -jar bsq-admin.jar` | MES 后端 |
| 8 | `java -jar thingsboard.jar` | IoT 平台 |
| 9 | `java -jar CoreApplication.jar` | DataEase |
| 10 | `40UIRun.bat` | Nginx 前端 |

首次启动建议等待 **3～5 分钟**。

### 4.4 单独启动脚本

| 脚本 | 用途 |
|------|------|
| `30启动平台服务.bat` | 仅 MES 后端 |
| `31启动IOT控制台.bat` | 仅 ThingsBoard |
| `32启动DataEase2.10.5.bat` | 仅 DataEase |
| `40UIRun.bat` | 仅 Nginx |
| `50停止平台UI.bat` | 停止 Nginx |
| `25启动dbeaver管理工具.bat` | 数据库 GUI |
| `11启动Redis管理工具.bat` | Redis 管理 |

---

## 5. 端口与默认凭据

| 服务 | 端口 | 地址 | 账号/密码 | 备注 |
|------|------|------|-----------|------|
| MES 前端 | **82** | http://127.0.0.1:82 | user01 / 123456 | 主入口 |
| MES API | 8080 | http://127.0.0.1:8080 | JWT 登录 | Nginx `/prod-api/` 代理 |
| DataEase | 8081 | http://127.0.0.1:8081 | admin / Admin123 | 数据可视化控制台 |
| ThingsBoard | 8090 | http://127.0.0.1:8090 | sysadmin@thingsboard.org / sysadmin123 | IOT 控制台 |
| MySQL (MES) | 3306 | localhost | root / 见下方说明 | 库名 `bsq-mes` |
| MySQL 8 | 8306 | localhost | root / 123456 | 库名 `dataeasev2` |
| PostgreSQL | 5432 | localhost | postgres / postgres123 | 库名 `thingsboard` |
| Redis | 6379 | localhost | 默认无密码 | DataEase 配置为 123456 |
| MinIO | 9000 | http://127.0.0.1:9000 | minioadmin / minioadmin | bucket: `bsq-mes` |
| ActiveMQ MQTT | 1893 | tcp://127.0.0.1:1893 | bsqmes / 123456 | MES 设备消息 |
| ActiveMQ 管理台 | 8161 | http://127.0.0.1:8161/admin | admin / admin | Web 控制台 |
| 标识解析代理 | 3000 | http://127.0.0.1:3000/api/ | — | 代理外部二级节点 |

### 5.1 密码配置注意事项

- 启动脚本提示 MySQL 密码为 `123456`，但 `bsq-admin.jar` 内配置为 `x5`，连库失败时需核对
- Redis：MES 无密码，DataEase 期望 `123456`，不一致可能导致 DataEase 异常
- 默认口令较弱，**不可直接用于生产环境**

---

## 6. MES 后端（bsq-admin）

基于 **Spring Boot + MyBatis + Druid**，架构类似 **若依（RuoYi）**。

### 6.1 内置模块

| 模块 | 职责 |
|------|------|
| `bsq-common` | 公共工具 |
| `bsq-framework` | 安全、权限、Web 框架 |
| `bsq-system` | 系统管理 |
| `bsq-mes` | 制造业务核心 |
| `bsq-quartz` | 定时任务 |
| `bsq-generator` | 代码生成 |

### 6.2 外部集成

| 组件 | 配置 |
|------|------|
| Redis | localhost:6379 |
| MinIO | http://127.0.0.1:9000，bucket `bsq-mes` |
| MQTT | tcp://127.0.0.1:1893，用户 bsqmes |
| ThingsBoard | http://127.0.0.1:8090 |
| DataEase | http://127.0.0.1:8081 |
| UReport | Nginx `/ureport` 反代 |

---

## 7. MES 业务模块

### 7.1 主数据 `md`

物料、BOM、SOP、分类、客户、供应商、车间、工作站、设备/工装/人员绑定、计量单位等。

### 7.2 生产管理 `pro`

工序、工艺路线、工单、任务排程（甘特图）、报工、排产计划。

### 7.3 质量管理 `qc`

IQC 来料检、IPQC 过程检、OQC 出货检、缺陷、检测模板与指标。

### 7.4 仓储管理 `wm`

仓库/库区/库位、库存、条码、入库、领料、成品入库、销售出库、调拨、退货、SN、装箱等。

### 7.5 设备管理 `dv`

设备台账、点检计划/记录、维修单。

### 7.6 排班日历 `cal`

班组、成员、班次、排班计划、节假日。

### 7.7 工装管理 `tm`

工装类型与台账。

### 7.8 报表 `report`

UReport 报表设计与展示。

### 7.9 系统管理 `system`

用户、角色、菜单、部门、字典、日志、自动编码等。

---

## 8. IoT 平台（ThingsBoard）

| 项目 | 说明 |
|------|------|
| 版本 | 3.3.4-SNAPSHOT |
| 端口 | HTTP 8090，MQTT 1883 |
| 数据库 | PostgreSQL `thingsboard` |
| 数据目录 | `data/` |

---

## 9. 数据可视化（DataEase）

| 项目 | 说明 |
|------|------|
| 版本 | 2.10.5 |
| 端口 | 8081 |
| 配置 | `opt/dataease2.0/config/application.yml` |
| 数据库 | MySQL 8 @ 8306，库 `dataeasev2` |

---

## 10. 前端（dist_mes）

Vue 2 + Element UI，Nginx 静态托管。内置 PDF.js。页面标题当前为「智慧购物」（演示定制名）。

---

## 11. 中间件职责

| 组件 | 作用 |
|------|------|
| Nginx | 静态托管、API 反代、标识解析代理 :3000 |
| ActiveMQ | MQTT 1893 |
| MinIO | 附件存储 |
| Redis | 缓存与会话 |
| MySQL 5.x | MES 业务库 |
| MySQL 8 | DataEase 库 |
| PostgreSQL | ThingsBoard 库 |

---

## 12. 标识解析代理

Nginx :3000 将 `/api/` 代理到 `http://iiot.nlecp.com:9591`，配置见 `nginx-1.22.1/conf/nginx.conf`。

---

## 13. 运维排查

| 现象 | 可能原因 |
|------|----------|
| :82 无法访问 | Nginx 未启动或端口占用 |
| API 502 | bsq-admin 未就绪 |
| MES 数据库错误 | MySQL 未启动或密码不一致 |
| DataEase 失败 | MySQL8 或 Redis 密码问题 |
| ThingsBoard 失败 | PostgreSQL 未启动 |

**日志：** `activemq/data/activemq.log`、`nginx-1.22.1/logs/`、`opt/dataease2.0/logs/dataease/`

另可参考 `平台服务问题排查.docx`。

---

## 14. 快速上手 Checklist

- [ ] 解压到纯英文路径
- [ ] 复制 `opt` 到根目录
- [ ] 运行 `一键启动工业互联网平台服务.bat`
- [ ] 等待 3～5 分钟
- [ ] 打开 http://127.0.0.1:82
- [ ] 登录 user01 / 123456

---

## 15. 相关文件

| 文件 | 说明 |
|------|------|
| `Readme.txt` | 简要部署说明 |
| `reademe.txt` | opt 目录说明 |
| `平台服务问题排查.docx` | 故障排查 |
| `nginx-1.22.1/conf/nginx.conf` | Nginx 配置 |

---

*文档基于运行包内 JAR、Nginx、前端 API 及启动脚本分析整理。*
