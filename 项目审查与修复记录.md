# MES 演示系统审查与修复记录

日期：2026-09-05

本次检查覆盖运行入口、前端资源及增强脚本、可移植 Vue 页面与 MQTT 桥接、独立排产领域源码、在线 IDE 集成、启动脚本、现有配置及日志。工程主体是运行包，Java 后端没有完整源码，因此本记录区分已验证的修复与尚未完成的运行验证。

## 保留的演示设计

- 保留“模块加载 → 生产工单 → 工序设置 → 工艺流程 → 生产排产 → 生产报工”的页面顺序与模块显示条件。
- 保留默认品类二、既有演示数量、方案生成进度、确认弹窗及原有下发接口。
- 保留品类二的七道工序、优先级与评分策略，以及在线 IDE 的隔离副本和模拟部署过程。
- 保留启动时重置演示导入数据的既有逻辑。本次没有启动后端、重置数据库或发送真实产线指令。
- 保留审查开始前工作区已有的修改；压缩资源按当前文件重新同步。

## 已修复的问题

| 位置 | 触发条件与原问题 | 修复结果 |
| --- | --- | --- |
| 运行版排产页面 | 下发成功只有提示，没有发送看板与报工监听的成功事件；空工单列表直接访问最后一条会报错；连续确认可能重复下发 | 成功后发送一次包含工单、品类、数量与方案快照的事件；空列表提示；确认到请求结束期间加锁，取消或失败后释放 |
| 排产增强脚本 | 展示的方案未保存到页面模型，后续联动缺少方案数据；方案编号仅取时间末四位，短时间内可能碰撞 | 保存预览快照，编号使用完整毫秒时间；下发成功重置方案；页面销毁清理定时器与监听 |
| Excel 导入 | 产品查询为模糊匹配，直接选第一条可能把 `BEER-02` 关联成 `BEER-020`；分页后可能找不到目标 | 按完整编码精确匹配，必要时继续查询后续页；部分导入失败显示成功数量及失败行，方便定位 |
| 工艺二维码 | 分页或编辑导致表格行复用后，按钮仍使用创建时的旧路线；路线文本直接拼进 HTML | 点击时读取当前路线；文本转义；关闭旧弹窗后异步结果不会写入新弹窗；二维码库加载失败后允许重试 |
| 看板与待报工联动 | 缓存为 `null` 或写满时中断；午夜/跨月后旧统计一直显示；重复成功通知可能重复建待报工项 | 容错读取并保留当前页面内存状态；按日/月刷新统计；同一方案的待报工项去重；删除待报工项后停止未发起的重试 |
| 独立排产源码：工作日历 | 相邻班次交接额外增加一分钟；不足一分钟的可用时间被舍弃；无效班次异常不明确 | 使用左闭右开区间与精确分钟数；班次边界直接衔接；校验日期、星期和班次时间，保留既有 UTC 约定 |
| 独立排产源码：计划范围 | 并行工序按数组首尾计算开始/结束，可能漏算较晚完成的工序并错误判断交期；默认日历与引擎不一致 | 汇总所有工序的最早开始和最晚结束；复用引擎约定的默认日历。教学代码行号清单仍对应原位置 |
| 可移植 MQTT 桥接 | 连接失败后旧客户端继续重连；后续请求再建同 ID 客户端；连接关闭但不报错时请求可能一直等待；异常剩余数量也能生成指令 | 为连接等待设置超时并关闭失败客户端，重连复用现有客户端；发起连接前校验剩余数量；拒绝非正整数工单 ID |
| 在线 IDE | 关闭/重试后旧健康检查响应仍继续操作或轮询；iframe 空白页和错误页的 load 也被当成编辑器就绪 | 关闭与新连接使旧请求结果失效；仅已有的 `mes-theia ready` 消息确认编辑器就绪 |
| 启动与运行资源 | 一键启动依赖的 PowerShell 文件被 Git 忽略；端口 82 已打开时跳过 IDE 检查；未等待 MQTT/MinIO 就启动业务；部分 gzip 副本落后于当前代码 | 纳入脚本跟踪范围，UI 已运行但 IDE 缺失时补启动，二者都运行时保留当前编辑状态，补齐依赖等待；刷新缓存版本并同步当前入口实际引用的压缩资源 |

## 验证结果

在项目目录执行：

```powershell
node --test tests/*.test.cjs
```

结果：**21 项通过，0 项失败**。

- 回归覆盖班次边界、节假日、占用时间窗、并行工序交期、默认日历、品类二完整七工序及同一订单复用。
- 直接提取当前入口所加载的编译后排产方法，验证空列表、重复确认、成功事件快照、取消和失败。接口为隔离桩，没有下发真实指令。
- 覆盖产品精确匹配与分页、缓存异常、日/月统计切换、IDE 旧响应失效、MQTT 超时/失败/重连与非法数量。
- 当前入口和懒加载 JavaScript 资源存在；361 个前端 JavaScript 文件及入口内联脚本语法通过；活动 gzip 与对应文件内容一致；演示更新包大小与清单一致。
- 9 个 PowerShell 脚本通过解析；可移植 Vue 组件 script 通过语法解析；`nginx -t` 通过；`git diff --check` 通过。

测试入口：`tests/mes-regression.test.cjs`、`tests/runtime-assets.test.cjs`。

## 验证边界

检查时平台服务未运行，后端包含启动即清理演示数据的逻辑。本次采取静态检查及隔离回归，没有执行真实数据库、真实 MQTT 设备或完整登录后的浏览器端到端测试。因此这些结果不能代表 Java JAR 内全部业务接口已经通过实机验证。

独立排产源码与 `dist_mes` 的演示排产页面原本就是隔离的：本次分别修复并验证，不把它们改造成新的业务链路。JAR、数据库内容及当前 IDE 中正在编辑的隔离副本均未替换；源码修复会在下次按原启动流程恢复 IDE 副本时带入。

现有页面打开后需刷新一次，以加载新的资源版本。若继续进行真实联调，应使用单独的演示数据副本执行完整链路，避免审查过程改变当前演示数据。

## 后续调整：大屏与下单工单关联（2026-09-05）

按用户截图要求，统计改为成功下单后启用。未下单时显示“—”和“待下单”；成功后按工单 ID 保存当前会话关联记录，并每 5 秒读取工单最新计划数量与已生产数量。同一工单重复通知不重复累加，工单删除后从统计移除，跨页面与刷新保留当前会话关联。旧版累计演示数据不再读取，也不再为了更新大屏将工单数量强制写为 100。

当日产量、当月产量分别汇总本日、本月已下单工单的已生产数量；当月计划汇总本月已下单工单的计划数量。运行时间从本会话首次成功下单起计时。能耗保留原有演示估算方式，仅在成功下单后显示。

运行版和可移植 Vue 页面的下单成功提示均去掉数量，仅显示“柔性排产方案已下发：品类名称”。已刷新实际浏览器验证未下单状态；成功后的数据对应关系、轮询只读请求、去重和提示文字通过隔离回归验证，没有为测试额外下发产线指令。更新后共 24 项测试通过。

## 后续调整：物料库存演示联动（2026-09-05）

大屏物料库存补入四类演示备料：包材仓空罐 12000、罐盖 12000，辅料仓标签 15000，包装仓纸箱 800。下单前显示备料数量和“待下单”；成功下单后采用与统计指标相同的工单记录，显示关联工单编号、剩余库存、领用减少量及同步状态。多个工单共同领用时，编号完整内容可悬停查看。

演示领用规则为每件产品一个空罐、一个罐盖、一张标签，每张工单按 24 件一箱向上取整。100 件工单对应剩余数量 11900、11900、14900、795。数量完全按工单计划计算，不采用隐藏下单输入框的默认值；每次从初始备料重算，刷新、轮询和重复通知不重复扣减。工单数量修改或删除后同步重算；超过备料量时显示缺料数量，不产生负库存。本次仅增加大屏演示数据，没有修改真实仓库台账或下单流程。

实际浏览器已刷新并确认备料表格显示正常。新增隔离回归覆盖下单前后数量、重复通知、多工单包装取整、工单修改/删除、缺料及表格渲染；共 28 项测试通过。已更新入口资源缓存版本并同步 gzip 文件。

## 后续调整：报警模块正常演示记录（2026-09-05）

保留“报警提醒”模块标题及编号、时间、级别、内容四列，补入系统就绪、备料加载、看板同步就绪、暂无报警四条正常提示。使用绿色正常标记，底部注明“演示数据”和“正常运行 / 无报警”。下单成功后，最近三张工单各自的接收、库存同步、计划同步记录显示在顶部，时间取工单成功下发时间；重复成功通知和轮询不会重复增加记录，较早记录可以滚动查看。

该模块只生成前端演示运行记录，没有接入设备报警接口、读取设备状态或写入真实报警记录。实际浏览器已确认布局及正常提示显示，新增隔离测试覆盖工单关联、去重、记录条数限制、时间与安全文本渲染，共 30 项回归测试通过。入口缓存版本及 gzip 副本已同步更新。

## 后续调整：设备稼动率演示图表（2026-09-05）

设备稼动率区域增加综合百分比、运行状态及灌装、封装、检测、码垛四个单元的横向条形图。未下单时显示 0% 和待机；成功下单后，按关联工单的剩余计划量生成确定性的演示负载，显示运行中和关联工单编号。100 件未生产工单对应四个单元 87%、91%、85%、89%，综合 88.0%。该百分比为演示模拟值，不是设备实测值或工单完成率，页面底部标明演示数据。

相同工单通知去重，按当前工单重算，轮询不累加百分比；所有关联工单完成后回到 0% 待机，数值始终位于 0–100%。没有增加任何设备接口请求。实际浏览器已验证待机图表布局；隔离回归覆盖下单、重复通知、工单完成、多工单及数值边界，共 31 项测试通过。入口缓存版本及 gzip 文件已更新。

## 后续调整：生产质量下单前后展示（2026-09-05）

生产质量模块在当前会话没有成功下单工单时显示“暂无数据”，隐藏原有半圆图形；成功下单后显示完整蓝色圆环与“100% 合格率”，附关联工单编号和演示数据标识。该值遵循用户指定的演示规则，不读取真实质量检测数据；重复下单通知保持 100%，全部关联工单删除后恢复无数据。

浏览器已验证未下单状态。现有回归用例增加质量值的初始、下单成功、重复通知及工单删除断言，31 项测试全部通过，缓存版本与 gzip 副本已同步。

## 后续调整：无需重启的演示重置（2026-09-05）

新增全局右下角“重置演示”入口。打开时只读取清理预览，列出演示工单、任务、报工及导入产品数量和工单编号；用户确认后才执行。范围限定为 `EXCEL_IMPORT` 工单及关联任务、报工，另包含由本演示排产脚本生成的自动报工，以及未被保留工单引用的 `EXCEL_IMPORT` 产品。正式工单、基础配置、工艺流程、登录及 IDE 代码保留。

采用现有业务接口按报工、任务、工单、产品顺序清理。工单删除接口要求草稿状态，因此只对已确认属于演示导入的工单恢复草稿后删除，保留其他字段；不调用生产执行或设备接口。执行前重新检查预览，数据变化则停止；失败不会清空浏览器记录或误报完成，可重新预览重试。该流程由多个业务接口组成，不是单一数据库事务，错误提示明确说明已完成的清理不会自动回滚。

成功后只移除大屏会话、演示待报工、旧累计统计及生产模块加载标记，返回首页；其他同源页签接收完成通知并同步刷新。重置期间暂停大屏和报工联动，等待当前报工请求结束，阻止延迟回调或重试重新写入演示数据。

隔离回归覆盖清理范围、保留被正式工单引用的产品、接口清理顺序、陈旧预览拒绝及异步回调暂停，共 35 项测试通过。实际浏览器已确认入口和预览可用，随后页面回到初始状态，再次读取预览时四类后台演示记录数量均为 0。缓存版本及 gzip 资源已同步。

重置入口位置调整：按用户要求取消右下角全局悬浮按钮，入口仅出现在“系统监控 → 定时任务”的工具栏，紧邻“日志”按钮，采用同款按钮样式。离开该页面移除入口，清理预览和重置逻辑保持不变。已在浏览器确认首页无悬浮入口、定时任务页按钮位置正确，35 项回归测试通过。

## 后续调整：重置同时恢复当前 IDE 代码（2026-09-05）

按用户新的要求，原“保留 IDE 代码”行为改为恢复固定基准。已将当前 `ide-workspace/bsq_usr` 的 14,374 个文件保存到 IDE 隔离边界外的 `ide-baseline/bsq_usr`，清单位于 `ide-baseline/manifest.json`，基准标识为 `880f6344cf5dbba39868d7afc3853f9300228d15f5ea5ccfd7736d0faebef8c5`。基准目录是必要的本地演示资源，不能删除；不会在每轮演示时重新捕获、覆盖为修改后的代码。

重置预览先读取基准信息。原数据清理完成后，通过同源 IDE 服务调用固定路径还原程序：校验基准文件哈希，恢复修改和删除的文件，移除新增文件或目录，只覆盖内容有变化的文件。文件操作限制在 IDE 隔离副本，符号链接不允许越界。实际启动脚本也优先使用这一固定基准，避免重启后恢复成另一版代码。

IDE 服务使用 MES 登录状态和现有工单清理权限验证请求，不接受外部传入的任意目录。还原失败时整个重置不会显示成功。浏览器重置通知会让已加载的新版本 IDE 丢弃编辑缓冲并关闭编辑器，同时清理该 IDE 的布局/编辑缓存及旧版在线编辑器缓存；成功后重新打开 IDE 会读取基准文件。

已完成扩展 TypeScript 编译、浏览器与服务端构建，并只重启 IDE 服务使其生效，MES 主服务继续运行。实际浏览器预览已显示基准文件数量及新的撤销说明。隔离文件测试覆盖修改、删除、新增、损坏基准阻止清理和目录链接保护，连同现有回归共 38 项通过。未为了验证额外清理当前业务数据。
